[package]
name = "forgeai"
version = "0.1.0"
edition = "2021"

[dependencies]

[[bin]]
name = "forgeai"
path = "src/main.rs"

[workspace]
members = [
    "common",
    "gateway",
    "cache",
    "batcher",
    "backend",
    "telemetry"
]
